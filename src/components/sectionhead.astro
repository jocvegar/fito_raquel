---
const { align = "center" } = Astro.props;
---

<div class:list={["mt-16", align === "center" && "text-center"]}>
  <h1 class="text-primary text-4xl lg:text-5xl font-bold lg:tracking-tight">
    <slot name="title">Title</slot>
  </h1>
  <p class="lg:text-xl text-lg mt-4 text-secondary">
    <slot name="desc">Some description goes here</slot>
  </p>
  <div data-aos="fade-down" class="w-12 h-1 bg-accent rounded my-6 mx-auto"></div>
</div>
